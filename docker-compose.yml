version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - MONGO_URI=mongodb://localhost:27017/kryptonix
    # Uncomment if using MongoDB container
    # depends_on:
    #   - mongo

  # mongo:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     MONGO_INITDB_DATABASE: kryptonix
  #   volumes:
  #     - mongo_data:/data/db

# volumes:
#   mongo_data:
